server:
  port: 8080
fcm:
  service-account-path: classpath:ecoulash-6862d356bdff.json
  project-id: ecoulash
spring:
  datasource:
    url: jdbc:postgresql://acela.proxy.rlwy.net:39352/railway
    username: postgres
    password: IBwAqLmKpTQJgUiLmUZFzsDiItHnuHbk
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

# ── Cloudinary ──────────────────────────────────────────────────────────────
cloudinary:
  cloud-name: dnv9ctnyk                  # Cloudinary dashboard dagi Cloud Name
  upload-preset: unsigned_shop_upload    # Unsigned upload preset nomi
  folder: ekoulash                       # Asosiy papka (avatars/ va products/ ichida)

# ── App sozlamalari ─────────────────────────────────────────────────────────
app:
  jwt:
    secret: "ekoulash-super-secret-key-2024-must-be-at-least-256-bits-long!"
    expiration: 86400000         # 24 soat (ms)
    refresh-expiration: 604800000 # 7 kun (ms)
  otp:
    expiry-seconds: 300          # 5 daqiqa
  telegram:
    bot-token: "YOUR_BOT_TOKEN_HERE"
  dev-mode: true                 # Developer rejimi — OTP response da ko'rinadi

logging:
  level:
    uz.ekoulash: DEBUG
    org.springframework.security: WARN